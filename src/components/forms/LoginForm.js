import { styled } from "@mui/material";
import React from "react";
import Grid from "@mui/material/Grid";
import { MainContainer } from "./RegisterForm";

function LoginForm() {
  return (
    <MainContainer item xs={12} md={5.8}>
      Login
    </MainContainer>
  );
}

export default LoginForm;

